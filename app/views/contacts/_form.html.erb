<%= form_for @contact do |f| %>
  <% if @contact.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@contact.errors.count, "error") %> prevented this message from being sent:</h2>
      <ul>
      <% @contact.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="contact_instructions">
    <p>If you wish to leave feedback about my site or contact me for any reason, please use the form below. If you would like me to contact you please leave details in the message body.</p>
    <p>Alternatively I can be contacted through <%= link_to "Twitter", "http://www.twitter.com/mellett68" %> or <%= link_to "Linkedin", "http://www.linkedin.com/in/pmellett" %>.</p>
    <p>Thanks.</p>
  </div>
  <div class="field">
    <%= f.label :subject %><br />
    <%= f.text_field :subject %>
  </div>
  <div class="field">
    <%= f.label :content, 'Your message' %><br />
    <%= f.text_area :content, :col => 40, :row => 30 %>
  </div>
  <div class="field">
  <%= f.hidden_field :source_ip %>
  </div>
  <div class="actions">
    <%= f.submit "Send Message" %>
  </div>
<% end %>