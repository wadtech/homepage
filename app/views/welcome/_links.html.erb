<ul class="nav nav-pills">
  <% info_links.each do |link_hash| %>
    <%= content_tag :li, :class=>(current_page?(link_hash[:path].downcase) ? 'active' : nil) do %>
      <%= link_to link_hash[:link_text], link_hash[:path] %>
    <% end %>
  <% end %>
</ul>