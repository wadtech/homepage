<% @projects.each do | project | %>
  <li>
    <div class="thumbnail">
      <%= link_to (image_tag project.image.url(:thumb), :alt => project.title), project %>
      <h5><%= project.title %></h5>
      <%= link_to "Read more", project %>
    </div>
  </li>
<% end %>